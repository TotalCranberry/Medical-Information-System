spring.application.name=Backend
spring.datasource.url=jdbc:mysql://localhost:3306/mis?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&useServerPrepStmts=true&cachePrepStmts=true
spring.datasource.username=root
spring.datasource.password=Dub@2216

spring.jpa.hibernate.ddl-auto=update
logging.level.org.hibernate.tool.schema=DEBUG


spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

spring.security.user.name=admin
spring.security.user.password=admin
# Show SQL queries (for dev)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Generate a random 256bit base64-encoded string for jwt.secret or use a strong safe secret
jwt.secret=FX/QDiZ/s4UA2Qt7GSr4aAukBVo3WZz1nhxd/BsNzBM=
# Token validity time in milliseconds (e.g., 1 hour)
jwt.expiration=3600000
logging.level.org.springframework.security=DEBUG

# Google OAuth2 configuration
google.client.id=793704402829-o85624o9bu1ti9u6mkagktkqe8h78kne.apps.googleusercontent.com

cors.allowed-origins=http://localhost:3000,https://your-production-domain.com

# ---- AES secret (Base64, 32 bytes => 256-bit) ----
app.crypto.secret=${APP_CRYPTO_SECRET}


# Database Configuration (add to existing application.properties)

# Prescription encryption configuration
app.prescription.encryption.secret-key=${PRESCRIPTION_SECRET_KEY:your-very-secure-secret-key-for-prescription-encryption-change-in-production}

# Prescription settings
app.prescription.overdue-hours=24
app.prescription.max-medications-per-prescription=20
app.prescription.enable-audit-log=true

# Logging configuration for prescriptions
logging.level.com.mis.service.PrescriptionService=INFO
logging.level.com.mis.service.security.PrescriptionEncryptionService=WARN
logging.level.com.mis.controller.PrescriptionController=INFO

# Security settings for prescription endpoints
app.security.prescription.max-requests-per-minute=30
app.security.prescription.require-patient-consent=false